# school-management-system-mern-app
#### Project Setup

In order to spin up the project, in the root create .env with these two variables, with your own values.

MONGO_URI
SECRET_TOKEN

After that run this command

```bash
npm install && npm run dev
```
